diff --git a/lib/models/__init__.py b/lib/models/__init__.py
index bee2082..1ad8421 100644
--- a/lib/models/__init__.py
+++ b/lib/models/__init__.py
@@ -1,4 +1,4 @@
-from .model import model_v0
+from .model import res18PaNew7Brm as model_v0
 
 
 model_factory = {
diff --git a/lib/models/model.py b/lib/models/model.py
index 6a8c0db..e6bd2b8 100644
--- a/lib/models/model.py
+++ b/lib/models/model.py
@@ -227,9 +227,7 @@ class CSFM(nn.Module):
         return ex_de + ex_con + im
  
 
-laplacian_kernel = nn.Parameter(torch.tensor(
-        [-1, -1, -1, -1, 8, -1, -1, -1, -1],
-        dtype=torch.float32).reshape(1, 1, 3, 3).requires_grad_(False).type(torch.cuda.FloatTensor))
+# laplacian_kernel removed from global scope
 
 class SpatialAttention(nn.Module):
     def __init__(self, kernel_size=3):
@@ -251,13 +249,16 @@ class SpatialAttention(nn.Module):
         return x * self.sigmoid(out)
 
 class LABRM(nn.Module):
-    def __init__(self,in_channels,out_channels,mode = 'train'):
+    def __init__(self,in_channels,out_channels,mode = 'train', n_classes=19):
         super(LABRM,self).__init__()
         self.mode = mode
       
-        self.conv1 = ConvBNReLU(out_channels,19,1,1,0)
+        self.conv1 = ConvBNReLU(out_channels,n_classes,1,1,0)
         self.conv2 = ConvBNReLU(in_channels,out_channels,1,1,0)
         self.sam = SpatialAttention(1)
+        
+        laplacian = torch.tensor([-1, -1, -1, -1, 8, -1, -1, -1, -1], dtype=torch.float32).reshape(1, 1, 3, 3)
+        self.register_buffer('laplacian_kernel', laplacian)
     def forward(self,low,high):
         scale = high.size(2) // low.size(2)
         high = F.interpolate(high,scale_factor = 2,mode = 'bilinear')
@@ -268,7 +269,7 @@ class LABRM(nn.Module):
         im_sam = self.sam(im)
         pre = self.conv1(im)
         pre = (pre.max(1)[0]).unsqueeze(1)
-        pre_lap = F.conv2d(pre.type(torch.cuda.FloatTensor), laplacian_kernel, padding=1)
+        pre_lap = F.conv2d(pre, self.laplacian_kernel, padding=1)
      
         y = pre_lap + im
         z = im_sam + pre
@@ -283,19 +284,16 @@ class LABRM(nn.Module):
             return t
 
 class ContextPath(nn.Module):
-    def __init__(self,*args, **kwargs):
+    def __init__(self, n_classes=19, *args, **kwargs):
         super(ContextPath, self).__init__()
         
-        
         self.resnet = Resnet18()
         self.araspp = arASPP(512,[3,5,7])
-#         self.arm16 = AttentionRefinementModule(256, 128)
-#         self.arm32 = AttentionRefinementModule(512, 128)
         
         self.ffm32_16 = CSFM(256)
         self.ffm16_8 = CSFM(128)
         
-        self.brm = LABRM(192,64)
+        self.brm = LABRM(192, 64, n_classes=n_classes)
         
         self.init_weight()
 
@@ -340,7 +338,7 @@ class res18PaNew7Brm(nn.Module):
     def __init__(self, n_classes, aux_mode='train', *args, **kwargs):
         super(res18PaNew7Brm, self).__init__()
 
-        self.cp = ContextPath(mode = aux_mode)
+        self.cp = ContextPath(n_classes=n_classes, mode=aux_mode)
 #         self.sp = SpatialPath()
 #         self.ffm = FeatureFusionModule(256, 256)
 #         self.conv_out = BiSeNetOutput(256, 256, n_classes, up_factor=8)
diff --git a/lib/models/resnet.py b/lib/models/resnet.py
index b6f7ae7..a6dd35e 100644
--- a/lib/models/resnet.py
+++ b/lib/models/resnet.py
@@ -85,7 +85,7 @@ class Resnet18(nn.Module):
         return feat4,feat_im,feat8,feat16,feat32
 
     def init_weight(self):
-        state_dict = modelzoo.load_url(resnet18_url)
+        state_dict = modelzoo.load_url(resnet18_url, map_location='cpu')
         self_state_dict = self.state_dict()
         for k, v in state_dict.items():
             if 'fc' in k: continue
